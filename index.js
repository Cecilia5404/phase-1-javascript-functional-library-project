function myeach(testArr, alert) {
    const  objValues = object.values(testArr)
    return  objValues.forEach(test => {
        alert(test)
    })
    return testArr;
}
function myMap(testArr,callback) {
    const objValues = object.values(testArr)
    callback = testArr.map(test => {
        
    })
}
